commeted:test base

 PDXC Env. :System.setProperty("webdriver.chrome.driver", chromedriverPath + "/" + chromeDriver);   

    		 
Loc Env :System.setProperty("webdriver.chrome.driver", projectPath+"/chromedriver/chromedriver.exe");   
	 	    
	 	    commeted:test base
	 	    
PDXC Env. 	    			if(headLessMode.equals("true")) {

Loc Env. 	    			if(headLessMode.equals("false")) {
  
PDXC Env. : {workspace} 

  mccm_automation\configurationFile.properties : config file change for unix and api 
    
    ------------
   Push to PDXC Env.
    
cd "C:\Users\prout21\git\Automation_Framework\MCCM\MCCM"
git remote add fb_develop https://github.com/prout21/MCCM-Automation.git
git remote
git status
git add .
git status
git commit -m "gittest push"
git push -u origin master
    
    