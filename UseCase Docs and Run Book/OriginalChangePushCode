commeted:test base

//	System.setProperty("webdriver.chrome.driver", chromedriverPath + "/" + chromeDriver);  --commted from dxc-vf GIT
 String projectPath = System.getProperty("user.dir");  -ADDED for dev
    		 
System.setProperty("webdriver.chrome.driver", projectPath+"/chromedriver/chromedriver.exe");  -ADDED for dev
	 	    
	 	    commeted:test base
	 	    
	 	    //			if(headLessMode.equals("true")) {
//				ChromeOptions chromeOptions = new ChromeOptions();
//				chromeOptions.addArguments("--no-sandbox");
//				chromeOptions.addArguments("--disable-dev-shm-usage");
//				chromeOptions.addArguments("--headless");
//				driver = new ChromeDriver(chromeOptions);
//			}
//			else{
//				driver = new ChromeDriver();
//			}

                 driver = new ChromeDriver();  -ADDED for dev
                 
                 
    mccm_automation\configurationFile.properties : config file change for unix and api 
    commeted = workspace added proj path